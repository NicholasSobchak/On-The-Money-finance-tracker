services:
  db:
    image: postgres:16-alpine
    container_name: onthemoney-db
    environment:
      POSTGRES_DB: onthemoney
      POSTGRES_USER: app 
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
